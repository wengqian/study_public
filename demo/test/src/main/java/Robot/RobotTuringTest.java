package Robot;import com.alibaba.fastjson.JSONObject;import java.io.OutputStream;import java.net.HttpURLConnection;import java.net.URL;import java.net.URLEncoder;import java.net.URLDecoder;import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;/** * Created by wengqian on 2017/4/12. * Test * 图灵机器人的接口 */public class RobotTuringTest {    public static String RobotTuringCallBack(String question) throws IOException {        String APIKEY = "fb95348c1d974a43ad2088796050bd95";//        String question = "你叫什么名字？";//这是上传给云机器人的问题        //String INFO = URLEncoder.encode("北京今日天气", "utf-8");        String INFO = URLEncoder.encode(question, "utf-8");        String getURL = "http://www.tuling123.com/openapi/api?key=" + APIKEY + "&info=" + INFO;        URL getUrl = new URL(getURL);        HttpURLConnection connection = (HttpURLConnection) getUrl.openConnection();        connection.connect();        // 取得输入流，并使用Reader读取        BufferedReader reader = new BufferedReader(new InputStreamReader( connection.getInputStream(), "utf-8"));        StringBuffer sb = new StringBuffer();        String line = "";        while ((line = reader.readLine()) != null) {            sb.append(line);        }        reader.close();        // 断开连接        connection.disconnect();//        System.out.println(sb);        return sb.toString();    }    public static void  main(String[]agrs){        String question = "你好？";        try {            String return_str = RobotTuringCallBack(question);//            System.out.println(return_str);            JSONObject jsonObject = JSONObject.parseObject(return_str);            System.out.println(jsonObject.getString("text"));        }catch (Exception e){            e.printStackTrace();        }    }}